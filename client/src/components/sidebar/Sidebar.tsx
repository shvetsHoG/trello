import classes from './Sidebar.module.css';

const Sidebar = () => {
    return (
        <div className={classes.wrapper}>
            <div>Sidebar</div>
        </div>
    );
};

export default Sidebar;
